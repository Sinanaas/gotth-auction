services:
  postgres:
    image: postgres
    container_name: gotth-auction
    ports:
      - "6500:5432"
    env_file:
      - ./app.env
    volumes:
      - postgres:/var/lib/postgre/data
volumes:
  postgres: